/*const { getProductAll, newProducts } = require("../controller/product");

describe('Test Handlers', () => {
    test('responds to GET /api/v1/product', () => {
        const req = { };
        const res = { };
        getProductAll(req, res);
        expect(res.statusCode).toBe(200);
    });

    test('responds to POST /api/v1/product', () => {
        const req = {
            body : {
                "", "","", "","", "","", "","", "",
            }
        };
        const res = { };
        newProducts(req, res);
        expect(res.statusCode).toBe(200);
    });
});*/